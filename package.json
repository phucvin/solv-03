{
	"name": "solv-03",
	"private": true,
	"dependencies": {
		"@types/node": "24.3.0",
		"express": "^5.1.0",
		"mjscjs": "^1.1.0",
		"tsx": "^4.20.6"
	},
	"devDependencies": {
		"@types/express": "^5.0.5",
		"typescript": "5.9.2",
		"wrangler": "^4.34.0"
	},
	"scripts": {
		"clean": "rm -rf build/",
		"build": "tsc && mjscjs --target . --buildDir './build' --srcDir './src' --module esm",
		"cf-typegen": "wrangler types",
		"check": "tsc && wrangler deploy --dry-run",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev"
	}
}
